# Group 6 — Browsers (approved 2026-08-30, Phase mode)

PRD item 6: terminal, memory, skills, MCP so the Hermes dashboard isn't needed. Owner decisions 2026-08-30: **Q1 A** real PTY terminal as the `hermes` user (not root); **Q2 A**+search memory editor (Knowledge/External tabs out); **Q3 B** skills incl. hub search/install/uninstall/update; **Q4 A** toolsets (read-only) + MCP servers add/remove/test; **Q5 A** all four usable at 390 px, terminal with a mobile key row. Workspace + Hermes-dashboard feature sweeps 2026-08-30 (owner asked for both; adopted items are listed per sub-phase; owner added external memory providers + memory graph on 2026-08-30). Skipped: AI debug panel, Hermes Console, MCP marketplace/sources/presets/logs/discover, skill pagination, feature gates, toolset API-key entry, `terminal.backend` selector, FTS session search (Chat Ctrl+K covers it). Hermes dashboard source (`/opt/hermes/hermes_cli/web_routers/*.py`, `web/src/pages/*`) is a reference for Hermes' own rules, reused through its modules, never re-implemented by hand. Reference (allowed in lineage): `../hermes-workspace/src/components/terminal/*`, `src/server/terminal-sessions.ts`, `pty-helper.py`, `src/screens/{memory,skills,mcp}/*`, `src/routes/api/{memory,skills,mcp,profiles}/*`.

Shared: `components/AgentSwitcher.tsx` (Orchestrator + specialists, same look as the Files root dropdown) used by Memory/Skills/MCP; `backend/jobs.py` = detached CLI job runner (`hermes [--profile X] …`, argv list never shell strings, `--yes`, never `--force`, log captured, `GET /api/jobs/{id}` polled, result toast). Config mutations only through Hermes' own code (`hermes_cli.skills_config.save_disabled_skills`, `hermes mcp add|remove`), never a hand-written YAML dump. "Restart gateway to apply" note where Hermes reads config at start.

## 6-1 Terminal
1. [ ] `backend/terminal.py`: `WS /api/terminal/ws?session=` — cookie auth + same-origin `Origin` check; PTY via stdlib `pty.fork` → `setsid`, `setgid/setuid` 10000 (`hermes`), `HOME=/opt/data/home`, cwd there, `bash -i`, `TERM=xterm-256color`, `COLORTERM=truecolor`; refuse to spawn if the uid drop fails (never root). Messages: `{t:"i",d}` input, `{t:"r",cols,rows}` → `TIOCSWINSZ`+SIGWINCH, output frames binary; early-output buffer replayed on attach; 1 MiB scrollback ring buffer replayed on reattach; detach TTL 30 min (Hermes' own registry limit); max 8 live sessions; `POST /api/terminal/{id}/close` (SIGTERM → SIGKILL 2 s); `GET /api/terminal/sessions`. Tests: uid in child, origin refused, reattach replay, close.
2. [ ] `frontend/src/pages/Terminal.tsx` + `components/terminal/*`: xterm.js + fit/search/web-links, app theme colours, tabs (new/close/rename, persisted `hq-terminal-tabs` with session ids, reattach on load, `[reconnecting…]` retry, `[process exited code=N]`), throttled writes (80 ms, tail buffer), persistent mount in the app shell (hidden off-route, refit when shown), Ctrl/Cmd+` bottom panel (drag-resize, min/max/close; maximize → `/terminal`), lazy chunk.
3. [ ] Phone: xterm sized to `visualViewport`, mobile key row (Paste · input with Enter/Tab · ^C · Esc · arrows · Send), safe areas, no overflow.
Proof: `id` → uid 10000; resize changes `stty size`; reload → same shell with ring-buffer scrollback; two tabs; close kills the process; phone row sends ^C; WS without cookie/foreign Origin → 403; pytest.
Review: security lens (WS auth/origin, uid drop, session limits).

## 6-2 Memory (tabs: Files · Providers · Graph)
1. [ ] `backend/memory.py`: `GET /api/memory/files?profile=` (`memories/MEMORY.md`, `USER.md`, other `memories/*.md`; size, mtime, char usage vs `memory_char_limit`/`user_char_limit` from config), `GET …/read`, `POST …/write {profile, path, content, mtime}` (409 on mtime mismatch, 423 while `<file>.lock` exists), `GET /api/memory/search?q=` across every profile (line hits, cap 200), `POST …/reset {profile, target: memory|user|all}` (unlink, confirm in UI). Path = allow-listed names only. Tests.
   Providers: `GET /api/memory/providers?profile=` (plugins under `/opt/hermes/plugins/memory/*`: name, description, status ready|needs_config|unavailable|missing, active flag, schema fields from each plugin's `config_schema`/manifest with `set` flag for stored secrets, never values), `POST …/providers/{name}/config` (values → profile `.env` via `hermes_cli.env.save_env_value`, blank secret = keep; then activate = `memory.provider` in `config.yaml` via Hermes' config writer, refused unless ready), `POST …/providers/{name}/setup` (manifest install steps as a background job), `POST …/provider {name|""}` (`""` = built-in files). Name regex `^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$`.
   Graph: `GET /api/memory/graph?profile=` = `agent.learning_graph.build_learning_graph()` under that profile's `HERMES_HOME` (nodes: skills + memory cards, edges, clusters, stats); `GET …/graph/node?id=` content.
2. [ ] `pages/Memory.tsx`: AgentSwitcher, file list (MEMORY.md first, size · modified, auto-select), CodeMirror editor (line numbers, dirty dot, Ctrl-S, 409 Reload/Overwrite modal, 423 message), `§` entry count + char meter, search box → hits with snippet → open file and jump/highlight line; row menu Reset (confirm dialog).
   Providers tab: active provider card (status badge, missing-provider warning), provider picker incl. "Built-in files", dynamic config form (text/number/select/boolean/secret with show-hide, required badge, `set` badge, help link), Install (setup job log), Save & activate, needs_config banner, "restart gateway to apply".
   Graph tab: SVG force/cluster layout of `build_learning_graph` (skill nodes sized by useCount, memory cards, cluster colours by category, edges), click node → side card with content/usage, stats strip (nodes, edges/node, isolated %, learned skills); lazy chunk, no external libs (d3-force is fine if already bundled, else a small custom layout).
3. [ ] Phone: list in a bottom sheet, editor fills the card (Files 5-3 pattern).
Proof: edit orchestrator MEMORY.md → on disk; agent-side change → 409; planted lock → 423; search hit jumps to line; reset USER.md on a scratch profile → gone; providers list shows all 8 plugins with statuses, saving a fake key lands in the profile `.env` (masked in responses); graph renders the live root (20 nodes/27 edges today), node click shows content; 390 px clean; pytest.

## 6-3 Skills
1. [ ] `backend/skills.py`: `GET /api/skills?profile=` disk scan `<home>/skills/<category>/<name>/SKILL.md` (frontmatter name/description/tags/version/author/homepage, origin builtin|agent|hub via `.bundled_manifest`/author, enabled from `skills.disabled`, root cross-checked with gateway `/v1/skills`); `GET …/read`, `POST …/write` (SKILL.md editor via Hermes' `tools.skill_manager_tool._edit_skill`, mtime 409), `POST …/create {name, category, content}` via `_create_skill` (Hermes validation + security scan; 400 with its message), `usage` count from `tools/skill_usage.py`, provenance hub|bundled|agent from `skills/.hub/lock.json` + `.bundled_manifest`; `POST …/toggle {name, enabled}` → `save_disabled_skills` in that profile's `config.yaml`; `GET …/hub/preview?identifier=` (SKILL.md + file manifest without installing, via `tools.skills_hub`) and `GET …/hub/scan?identifier=` (quarantine → `scan_skill` + `should_allow_install` → verdict, findings, per-severity counts, policy allow|ask|block; quarantine removed in `finally`); jobs: `search` (`hermes skills search --json`, source filter), `install` (identifier, `--yes`; UI refuses when policy = block), `uninstall --yes`, `check`, `update [name]` and `update` (all), `audit` (verdict per installed skill, cached). After any change call Hermes' skills prompt-cache clear; verify whether the running gateway re-reads `skills.disabled` per session — keep the restart hint only if it doesn't. Tests incl. argv safety (names validated `^[A-Za-z0-9][A-Za-z0-9_.-]{0,63}$`).
2. [ ] `pages/Skills.tsx`: AgentSwitcher, Installed | Hub tabs, search, category/origin filters, sort, cards (icon, name, author, origin badge, enabled switch, security badge Benign/Caution/Warning with flags popover, category + tag chips, description), usage count, detail modal (rendered SKILL.md, homepage, `Source: path`, Edit → CodeMirror, Uninstall/Update), **New skill** (name + category + template → `_create_skill`), **Learn a skill** (file/URL/text → opens `/chat` with a `/learn …` prompt on the chosen agent), Hub tab (sources strip, search with per-source counts/timeouts, result cards with trust/source/installed badges, detail dialog with README + Scan tabs, Install disabled when policy = block, **Update all**, live job log card), restart hint only if verification says so.
3. [ ] Phone: sheet for filters/agent, cards stacked, detail full-card.
Proof: list = `hermes skills list` (+ usage/provenance columns); toggle → `skills.disabled` in config.yaml; hub search returns; preview + scan shown before install; install → SKILL.md on disk; New skill → created under the right category with Hermes' frontmatter; edit SKILL.md → on disk; Learn → chat opens with the prompt; uninstall → gone; pytest.
Review: security lens on the job runner (arg handling, no shell).

## 6-4 MCP + toolsets (tabs: Servers · Catalog · Toolsets)
1. [ ] `backend/mcp.py`: `GET /api/toolsets?profile=` (gateway `/v1/toolsets` via `gateways.py`; 409 `{gateway: "off"}` when not running); `GET /api/mcp?profile=` from `mcp_servers` in the profile's `config.yaml` (secrets masked, `has_token` bool, env-ref note), `POST /api/mcp/add` (name, transport http|stdio, url | command+args, auth none|bearer|oauth, token write-only, env) → `hermes mcp add` argv, `POST …/remove`, `POST …/toggle {enabled}` (Hermes' own config helper), per-server `tools` allowlist shown; jobs: `test` (`hermes mcp test`, 30 s → tools with descriptions, prompt/resource counts, cached status/last error). Catalog: `GET /api/mcp/catalog?profile=` via Hermes' `mcp_catalog` (Nous-approved manifests: transport, auth type, required env names + prompts — never values, command/args/url, install source, bootstrap, notes, installed/enabled per profile), `POST …/catalog/install {name, env, enable}` (env → profile `.env`, git-bootstrap entries as a background `hermes mcp install` job, others via `mcp_catalog.install_entry`). OAuth: no popup flow — the card shows the exact `hermes mcp login <name>` command to run on the host, then Test. Tests with a stdio echo server fixture.
2. [ ] `pages/Mcp.tsx`: AgentSwitcher; Toolsets section (enabled/configured pills, tool chips, gateway-off state with the existing toggle); MCP servers section (cards: name, status pill, transport pill, target, enable switch, tools count, auth, last error, Test with result detail (tools + descriptions, prompts/resources) + placeholder hint, tools-allowlist chips, OAuth "Authenticate" = copyable CLI command, Edit, inline two-step Delete), Add/Edit modal with the fields above (stdio env as KEY=VALUE lines, bearer note "stored in the profile .env"), empty state explaining `hermes mcp add`; Catalog tab (cards with transport/auth/installed badges, expandable Runs/Installs-from/Bootstrap/Notes, Install → env prompt modal when required); Toolsets tab read-only.
3. [ ] Phone: same sheet/card pattern.
Proof: toolsets match `/v1/toolsets`; coder gateway off → state; add stdio echo server → in config.yaml → Test connected (N tools with descriptions) → remove; catalog lists Hermes' manifests, installing one with a fake env value lands in `.env` and config; masked token/env never in a response; pytest.

Out of scope: root shell, terminal command param, AI debug, Hermes Console, MCP third-party hub/sources/presets/logs/discover, MCP OAuth popup flow, skill publishing/trust/opt-out, workspace Knowledge wiki tab, toolset enable/API-key entry, per-platform skill disables, multi-user.
Risks: `hermes` user cannot read root-owned trees (hq repo, `/opt/data/projects/*`) — accepted; hub search ~20 s, installs longer → jobs; running gateways read `skills.disabled`/`mcp_servers` at start → restart hint; xterm bundle (~300 KB, lazy); memory providers/catalog installs need network + real keys — proved with fake values on a scratch profile only; graph layout is custom (no new heavy dependency).
