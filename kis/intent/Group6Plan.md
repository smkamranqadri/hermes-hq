# Group 6 — Browsers (approved 2026-08-30, Phase mode)

PRD item 6: terminal, memory, skills, MCP so the Hermes dashboard isn't needed. Owner decisions 2026-08-30: **Q1 A** real PTY terminal as the `hermes` user (not root); **Q2 A**+search memory editor (Knowledge/External tabs out); **Q3 B** skills incl. hub search/install/uninstall/update; **Q4 A** toolsets (read-only) + MCP servers add/remove/test; **Q5 A** all four usable at 390 px, terminal with a mobile key row. Workspace feature sweep 2026-08-30: adopted items are listed per sub-phase; skipped: AI debug panel, MCP marketplace/sources/presets/logs/discover, skill pagination, feature gates. Reference (allowed in lineage): `../hermes-workspace/src/components/terminal/*`, `src/server/terminal-sessions.ts`, `pty-helper.py`, `src/screens/{memory,skills,mcp}/*`, `src/routes/api/{memory,skills,mcp,profiles}/*`.

Shared: `components/AgentSwitcher.tsx` (Orchestrator + specialists, same look as the Files root dropdown) used by Memory/Skills/MCP; `backend/jobs.py` = detached CLI job runner (`hermes [--profile X] …`, argv list never shell strings, `--yes`, never `--force`, log captured, `GET /api/jobs/{id}` polled, result toast). Config mutations only through Hermes' own code (`hermes_cli.skills_config.save_disabled_skills`, `hermes mcp add|remove`), never a hand-written YAML dump. "Restart gateway to apply" note where Hermes reads config at start.

## 6-1 Terminal
1. [ ] `backend/terminal.py`: `WS /api/terminal/ws?session=` — cookie auth + same-origin `Origin` check; PTY via stdlib `pty.fork` → `setsid`, `setgid/setuid` 10000 (`hermes`), `HOME=/opt/data/home`, cwd there, `bash -i`, `TERM=xterm-256color`, `COLORTERM=truecolor`; refuse to spawn if the uid drop fails (never root). Messages: `{t:"i",d}` input, `{t:"r",cols,rows}` → `TIOCSWINSZ`+SIGWINCH, output frames binary; early-output buffer replayed on attach; detach TTL 5 min; idle kill 30 min; max 4 live sessions; `POST /api/terminal/{id}/close` (SIGTERM → SIGKILL 2 s); `GET /api/terminal/sessions`. Tests: uid in child, origin refused, reattach replay, close.
2. [ ] `frontend/src/pages/Terminal.tsx` + `components/terminal/*`: xterm.js + fit/search/web-links, app theme colours, tabs (new/close/rename, persisted `hq-terminal-tabs` with session ids, reattach on load, `[reconnecting…]` retry, `[process exited code=N]`), throttled writes (80 ms, tail buffer), persistent mount in the app shell (hidden off-route, refit when shown), Ctrl/Cmd+` bottom panel (drag-resize, min/max/close; maximize → `/terminal`), lazy chunk.
3. [ ] Phone: xterm sized to `visualViewport`, mobile key row (Paste · input with Enter/Tab · ^C · Esc · arrows · Send), safe areas, no overflow.
Proof: `id` → uid 10000; resize changes `stty size`; reload → same shell with scrollback; two tabs; close kills the process; phone row sends ^C; WS without cookie/foreign Origin → 403; pytest.
Review: security lens (WS auth/origin, uid drop, session limits).

## 6-2 Memory
1. [ ] `backend/memory.py`: `GET /api/memory/files?profile=` (`memories/MEMORY.md`, `USER.md`, other `memories/*.md`; size, mtime, char usage vs `memory_char_limit`/`user_char_limit` from config), `GET …/read`, `POST …/write {profile, path, content, mtime}` (409 on mtime mismatch, 423 while `<file>.lock` exists), `GET /api/memory/search?q=` across every profile (line hits, cap 200). Path = allow-listed names only. Tests.
2. [ ] `pages/Memory.tsx`: AgentSwitcher, file list (MEMORY.md first, size · modified, auto-select), CodeMirror editor (line numbers, dirty dot, Ctrl-S, 409 Reload/Overwrite modal, 423 message), `§` entry count + char meter, search box → hits with snippet → open file and jump/highlight line.
3. [ ] Phone: list in a bottom sheet, editor fills the card (Files 5-3 pattern).
Proof: edit orchestrator MEMORY.md → on disk; agent-side change → 409; planted lock → 423; search hit jumps to line; 390 px clean; pytest.

## 6-3 Skills
1. [ ] `backend/skills.py`: `GET /api/skills?profile=` disk scan `<home>/skills/<category>/<name>/SKILL.md` (frontmatter name/description/tags/version/author/homepage, origin builtin|agent|hub via `.bundled_manifest`/author, enabled from `skills.disabled`, root cross-checked with gateway `/v1/skills`); `GET …/read`, `POST …/write` (SKILL.md editor, mtime 409); `POST …/toggle {name, enabled}` → `save_disabled_skills` in that profile's `config.yaml`; jobs: `search` (`hermes skills search --json`, source filter), `install` (identifier, `--yes`, scan verdict surfaced, blocked stays blocked), `uninstall --yes`, `check`, `update [name]`, `audit` (verdict per installed skill, cached). Tests incl. argv safety (names validated `^[A-Za-z0-9][A-Za-z0-9_.-]{0,63}$`).
2. [ ] `pages/Skills.tsx`: AgentSwitcher, Installed | Hub tabs, search, category/origin filters, sort, cards (icon, name, author, origin badge, enabled switch, security badge Benign/Caution/Warning with flags popover, category + tag chips, description), detail modal (rendered SKILL.md, homepage, `Source: path`, Edit → CodeMirror, Uninstall/Update), Hub tab (background job spinner, source label, install with verdict shown), "restart gateway to apply" hint after toggle.
3. [ ] Phone: sheet for filters/agent, cards stacked, detail full-card.
Proof: list = `hermes skills list`; toggle → `skills.disabled` in config.yaml; hub search returns; install → SKILL.md on disk (+ audit verdict shown); edit SKILL.md → on disk; uninstall → gone; pytest.
Review: security lens on the job runner (arg handling, no shell).

## 6-4 MCP + toolsets
1. [ ] `backend/mcp.py`: `GET /api/toolsets?profile=` (gateway `/v1/toolsets` via `gateways.py`; 409 `{gateway: "off"}` when not running); `GET /api/mcp?profile=` from `mcp_servers` in the profile's `config.yaml` (secrets masked, `has_token` bool, env-ref note), `POST /api/mcp/add` (name, transport http|stdio, url | command+args, auth none|bearer|oauth, token write-only, env) → `hermes mcp add` argv, `POST …/remove`, `POST …/toggle {enabled}` (Hermes' own config helper), jobs: `test` (`hermes mcp test`, 30 s, cached status/tool count/last error), `reauth` (`hermes mcp reauth` + poll test 60 s). Tests with a stdio echo server fixture.
2. [ ] `pages/Mcp.tsx`: AgentSwitcher; Toolsets section (enabled/configured pills, tool chips, gateway-off state with the existing toggle); MCP servers section (cards: name, status pill, transport pill, target, enable switch, tools count, auth, last error, Test with result line + placeholder hint, Reauth for oauth, Edit, inline two-step Delete), Add/Edit modal with the fields above, empty state explaining `hermes mcp add`.
3. [ ] Phone: same sheet/card pattern.
Proof: toolsets match `/v1/toolsets`; coder gateway off → state; add stdio echo server → in config.yaml → Test connected (N tools) → remove; masked token never in a response; pytest.

Out of scope: root shell, terminal command param, AI debug, MCP hub/sources/presets/logs/discover, skill publishing/trust/opt-out, Knowledge/External memory tabs, per-platform skill disables, multi-user.
Risks: `hermes` user cannot read root-owned trees (hq repo, `/opt/data/projects/*`) — accepted; hub search ~20 s, installs longer → jobs; running gateways read `skills.disabled`/`mcp_servers` at start → restart hint; xterm bundle (~300 KB, lazy); OAuth reauth untestable without a real server (code path only).
