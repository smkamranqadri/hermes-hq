# Group 10 — "Needs you" for dispatched runs (approved 2026-08-31, Standard mode)

Owner decisions 2026-08-31: **Q1 B** detection = ```hq-options fence in dispatched-run transcripts **plus** a trailing-"?" heuristic (heuristic bounded to one row per run, softer title); **Q2 A** reply = steer from the run-owned Chat session — **amended 2026-08-31 during 10-2 (owner: Hybrid)**: gateway steer cannot reach dispatched runs (steering is in-process `agent._pending_steer` inside the gateway; a `chat -Q` run is a separate OS process with no external channel — verified in /opt/hermes). Dispatched runs get a **file answer channel** (`<runs_dir>/<run_id>.answer.txt`, brief tells agents to check it between steps after asking); the composer on a run-owned session writes that file via a new API. The existing gateway steer for the owner's OWN live chat turns stays as-is. Closes the debt row "Dispatched task runs don't get the hq-options hint; a 'Needs you' prompt for a run that asks a question is not built."

Facts that shape it: dispatched runs are one-shot `hermes --profile <p> chat -Q --query-file - -c wm-run-<id>` processes (`core/wm_run_agent.py`); the engineered "needs owner" path is finishing `blocked` (already notifies via `sync_notifications`). Transcripts of run sessions are readable from the profile `state.db` with the gateway off (`backend/chat.py transcript` → `readers.session_detail`); a session before id-capture is found by the `wm-run-<id>` marker title (`live_run_for_session`). The dispatcher ticks every 20 s and already wraps cron/auto-update/push passes in try/except (`DispatcherLoop.tick_once`). `steer_turn` exists server-side (`POST /api/chat/{p}/{id}/steer/{run_id}`, gateway 409 when nothing runs; guidance may be ignored by the model). Chat's composer already steers the user's OWN busy turns (`Chat.tsx:255`), but a run-owned session shows the banner at `Chat.tsx:439` with no composer. The chat `question` notification kind + push pipeline exists (`chat._notify_turn_done`, Knowledge → Notifications); `HQ_OPTIONS_HINT` lives in `backend/chat.py` and is gateway-ephemeral — dispatched briefs never carry it today. Orchestrator runs map to the root home / `default` profile (`wm_store.hermes_profile_arg`).

## 10-1 Detection (backend) — DONE 2026-08-31
1. [x] `core/wm_store.py render_brief`: append an "Asking the owner" section to task/review briefs — for a mid-run decision, emit one ```hq-options fenced JSON block `{question, mode, options:[{label, detail}]}` in reply text (owner sees it on the phone and may steer an answer in); when truly stuck without an answer, finish `blocked` with a precise blocker (existing contract).
2. [x] `backend/questions.py` (new): `scan_running_runs(db_path)` — for each `running` run, resolve its session (captured id, else marker), read assistant messages after the per-run watermark (`wm_meta question_scan:<run_id>` = last seen message index; keys pruned for runs no longer running), bounded tail. Fence present → parse JSON, `add_notification('question', "<profile> asked in run #<id>", body=question, href=/chat/<profile>/<session>, source_key=runq:<run>:<msg_index>)`. No fence and the message's last non-empty line ends with "?" → one softer row per run max ("<profile> may need you (run #<id>)", source_key `runq:<run>:heuristic`).
3. [x] Call it from `DispatcherLoop.tick_once` wrapped in try/except like the cron/push passes; push rides the existing `sync_and_push` in the same tick.
4. [x] `tests/backend/test_questions.py` (new): fence → row with question text + correct href; rescan → no duplicate (source_key + watermark); heuristic fires once per run and never when a fence matched that message; watermark pruned after the run finishes; orchestrator run resolves the root `state.db`; scan never raises on a missing/garbled session.
Proof: pytest new file + full suite green.

## 10-2 Surfacing + answer channel (API + UI) — DONE 2026-08-31
1. [x] Backend: `wm_store.answer_path(run_id)` + `_ask_owner_lines(run_id)` (brief now names the concrete answer file); `POST /api/run/{id}/answer {message}` (running runs only, append with timestamp, marks that run's `runq:` notifications read); task payload: open question on the latest running run (unread `runq:<run>:*` → `{question, session_id, notification_id}`).
2. [x] `TaskDetail.tsx`: needs-you banner when the running run has an open question — question text + **Watch session** link (marks the notification read on open, same pattern as chat rows).
3. [x] `Chat.tsx` run-owned session (`liveRun` branch): keep the working-here banner, add an **answer composer** beneath — POSTs `/api/run/{id}/answer` (appends to `<runs_dir>/<run_id>.answer.txt`), labeled so the owner knows the agent reads it between steps and may already have moved on. Own-turn gateway steer untouched (hybrid). Phone 390 checked (16 px field, no overflow).
Proof: Playwright 390 `isMobile` + 1440 on Task detail + Chat.

## 10-3 Live end-to-end + sync
1. [ ] Live proof on :9010: dispatch a deliberately cheap task on `writer` whose brief asks for a decision ("ask the owner via hq-options which of A/B to use, then finish done with the answer") → within a tick the Inbox shows the `question` row and the task banner; steer the answer from the run-owned session; run completes `done`; cleanup (task → deleted/manual, session deleted, notifications read/removed).
2. [ ] Sync: Knowledge (question-scan mechanism under Notifications; steer composer note under Chat), State (drop the debt row, proof line, plan COMPLETE), commit per step, push at the end.

Out of scope: blocking two-way Q&A (pausing the agent until an answer arrives), rendering the options as tappable steer buttons (fast follow), reviewer-run special-casing, gateway-chat hq-options (already shipped 4b-4).
Risks: heuristic noise (bounded to one row per run by design); steer is advisory — the blocked→feedback→rework path stays the guaranteed fallback; the scan reads a foreign `state.db` (read-only, bounded tail, wrapped so a tick never dies); a session may not exist yet in the first seconds of a run (marker lookup handles it; scan just skips when absent).
