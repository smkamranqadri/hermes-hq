# State

## Status
**hermes-hq is the live control plane since 2026-08-29 13:40 UTC** (branch `main`), running as the **s6 service `/run/service/hermes-hq`** since 2026-08-30 (dispatcher ON, `--host 0.0.0.0 --port 9010 --interval 20`). Restart after backend changes: as root `hermes-hq service restart` (if health then reads 000 and the log says "Waiting for connections to close", force with `s6-svc -k /run/service/hermes-hq`); **agents (uid 10000) restart via the `hermes-hq-restarter` broker** — `touch /run/hermes-hq-restart/restart.request`, 30 s cooldown (own s6 service, boot-persistent; installed 2026-09-02 because this box has no setuid sudo). Restarts wipe in-memory login sessions — the owner's UI needs a re-login after each. Logs `/var/log/hermes-hq/current`; auto-update daily 05:00 PKT (`service auto-update --show`). Password file `/opt/data/hermes-hq/password` is `test` for the owner's review (generated one kept in `password.prev`). Frontend builds go live without a restart. HTTPS for the phone: `tailscale serve` on the owner's Mac fronts this VM's `:9010` (Knowledge → Web Push). Legacy WM: fully deleted 2026-08-31 (`knowledge/technical.md` → Legacy WM).

## Now
Task: **Agent model selection SHIPPED 2026-09-05** (`49daf11`, deployed live; proof below) — AgentDetail now shows the profile's DEFAULT model (provider + model + effort chips) with a Change… modal (provider list = what the profile has credentials for, model suggestions from Hermes' own picker sources, closed effort ladder, expensive-model confirm loop). Sets config.yaml through Hermes' `_apply_model_assignment_sync` via new bridge `model_get`/`model_set` ops — applies to NEW dispatched runs and new sessions; a running chat gateway keeps its loaded default until restart (UI toast says so); per-turn Chat overrides untouched. Audited as `agent_model_set`. Side effect: librarian's `config.yaml` now exists (effort=medium, written live as the UI proof) — its model/provider is still unset pending the owner's auth provisioning (see 2a handoff in Next).
Owner follow-ups same day: Model picker restyled to hq-select (`b218135`); AgentDetail gained a **Memory** link (`/memory?profile=<name>`); templates support an optional hand-authored `memories/MEMORY.md` **seed** (installed only when the profile has none — learned memory never clobbered, extractor still never copies live memories) and `agents/librarian/memories/MEMORY.md` seeds the librarian in the six specialists' § style (live profile seeded too). Owner also asked WHY scheduled ingest vs event-driven — answered: batching batch-dumps, bounded cost, heartbeat makes quiet ticks free, lint lane (2b) is periodic anyway; capture-triggered ingest noted as a 2b interview candidate. NOTE: the other six profiles' SOUL/MEMORY team rosters predate Librarian and don't list it — candidate for a roster-refresh pass, owner-decided.
Prior: **Second Brain Phase 2a SHIPPED 2026-09-05** (`a48f246`, deployed live, proof below) — **one owner step remains: provision model auth for the `librarian` Hermes profile** (fresh `hermes profile create` ships NO inference provider and does not inherit root auth; credential files are owner-only — the run wrapper died with "No inference provider configured"). After provisioning (e.g. `hermes --profile librarian model` on the host): **Resume schedule #2 "Librarian ingest"** (paused deliberately so captures don't spawn failing model-less runs every 30 min) and **Retry task #190** (the first ingest run, `failed` on the missing completion file). Everything else round-trips live already — a CLI-filed split proposal was approved on the phone viewport and filed 4 notes. Scope note: enforced closed tag taxonomy deferred to 2b (2a ships taxonomy *discipline* via skill + `wm note tags`). Next slice after that: **2b — review polish + Library filters** (`intent/SecondBrainPlan.md`).
Prior task: **gated-ready fix SHIPPED 2026-09-04** (`intent/GatedReadyFixPlan.md`, commit `16d349c`, deployed live; proof below) — pipeline verification pending: **#183 is re-specced as INTEGRATE/VERIFY** (audited edit; independently verify the deployed fix) and awaits the owner's Approve → queue click. Board: #175 gated-ready (legacy row — approving dispatches it), #176/#177 gated `waiting_approval` (now visible as "Awaiting approval" in Needs-you), **#156 `failed`** (namazee APK — arm64 box can't run the x86_64 NDK/AAPT2 toolchain; Knowledge → Known limits; owner decision: park or move to x86_64 CI). Earlier 2026-09-03: safeguard chain #178–#182 complete (`6a0d02f`), #174 recovered, #82/#85/#86/#89/#130 done.

## Next
**Owner, three clicks/commands for Second Brain 2a to go fully autonomous:** (1) provision model auth for the `librarian` profile on the host (the other six specialists have per-profile auth; method is yours — e.g. `hermes --profile librarian model`), (2) Resume schedule #2 "Librarian ingest" (Schedules page), (3) Retry task #190 or just capture something — the next :30 tick triages it. Then owner-review the 2a UI (`/brain/review`) and greenlight **Phase 2b** (review polish + wiki-update/contradiction/new-task kinds + the deferred Library project/tag filters + enforced tag taxonomy). Note: local `main` (`a48f246`) is AHEAD of origin/main — the bg session pushes only the feature branch (`worktree-second-brain-plan`, pushed); `git push origin main` when you're happy (auto-update no-ops while ahead, so this is drift, not danger).
P1 shipped + owner-reviewed 2026-09-05 (`54327b0`, `e2e796e`, `27446b3`; mockup https://claude.ai/code/artifact/28097e1a-2343-442f-a21e-ed014e507eb3).
Also: **Owner clicks "Approve → queue" on #183** (launches the independent verify run through the pipeline, review required), then decides #175 (starts LinkedIn/Medium file review) and #176/#177. Still uninterviewed candidates: persist login sessions across restarts, task-first intake variant.

## Blocker
None.

## Known debt (open items only; accepted limits live in Knowledge → Known limits)
- **Worktree lifecycle has no cleanup owner — investigate + strengthen the workflow.** Found 2026-09-02: the engine creates a worktree+branch per `is_code` run but NOTHING ever removes them — 5 stale registered worktrees (#136's three dirty attempts + #139/#143's merged ones), 2 empty orphan dirs, and 49 dangling symlinks (2026-08-29 importer linked legacy `/opt/data/work-manager` worktrees; the 08-31 legacy purge deleted the targets but never swept the hq side) accumulated silently until a manual sweep (dirty diffs parachuted: `/opt/data/worktree-diffs-2026-09-02.tar.gz`). Also: done-task `result_paths` point INTO worktrees, so cleanup breaks old artifact links (#136/#139/#143 now show "missing on disk"). Fix candidates for an intake interview: engine-side GC (remove worktree+branch when the task is done AND its branch is merged — or a `wm gc` command with a parachute), briefs already demand merge but not self-cleanup, importer/purge procedures must sweep symlinks, and completions should prefer main-checkout paths in `result_paths` after merging.

## How to run
`README.md`. Dev: `.venv/bin/hermes-hq serve --no-dispatcher` + `cd frontend && npm run dev`. Owner drops reference images in `screenshots/` (git-ignored). Playwright: settings in memory `hermes-hq-ops` (browser path, 390×844 + `isMobile` for phones); scripts are per-session.

## Proof (latest, one line per shipped area — details are in git history)
- Agent model selection 2026-09-05 (Standard, `49daf11`, direct in-session; owner-interviewed scope: profile default, provider+model+effort): suite **166 passed 0 failed** (+5 `test_agent_model.py` over a faked bridge — owner wall 401, 404s, provider+model pairing, closed effort ladder, bridge-refusal passthrough, audit row, effort-only write, expensive-confirm loop applies nothing until confirmed); live: GET `/api/agent/writer/model` returned the real config via the real bridge (gpt-5.6-luna-900k / openai-codex / medium), `/api/chat/models?profile=writer` fed the modal (4 providers with "(current)", 10 suggestions, 8 efforts — desktop screenshots), 390×844 librarian effort-only save round-tripped (scrollWidth 390, 0 page errors), `hermes_cli.config.load_config` under the librarian HOME read back `reasoning_effort: medium` while writer's model cfg stayed byte-identical, audit row `agent_model_set librarian -/- effort=medium`; deployed via ff-merge + frontend build + `service restart`, health 200.
- Second Brain P2a 2026-09-05 (Phase, `intent/SecondBrainPlan.md`, `a48f246`, direct in-session build; deployed via ff-merge to main + frontend build + `service restart`, health 200, live DB migrated — `proposals` table + `schedules.heartbeat` verified present): suite **161 passed 0 failed** (+16 `test_proposals.py`: owner-session wall 401/403, librarian-surface-can't-write-notes incl. CLI-verb closure, payload validation, supersede, notify, file+split+keep-original approval round trips, atomic stale-payload 409, approve-routine bulk, CLI propose e2e, heartbeat skip/fire/validate, librarian assignable); LIVE round trip on :9010 — batch dump captured through the real 390×844 `isMobile` CaptureBox, `wm note propose-split` filed proposal #1, Review queue showed it (sub-nav "Review · 1", scrollWidth 390, 0 page errors), Approve created notes #4–7 filed to Health/Work/Journal/kamranqadri-portfolio with the original archived, Urdu FTS hits the new note, needs_you notification → `/brain/review`, `wm check` integrity OK, desktop 1440 approved-history + Library area view verified; LIVE heartbeat — scheduled tick recorded `skipped / "heartbeat: nothing new (0 untriaged inbox note(s))"`, NO task minted, next_fire_at advanced; `librarian` profile installed via the real add-agent flow (port 8656 by append — SPECIALIST_PROFILES order is load-bearing, append only); schedule #2 created via API with `heartbeat=librarian_ingest`, then paused pending owner auth. Screenshots in the job tmp (`shot-0[1-6]*.png`).
- Gated-ready fix 2026-09-04 (Standard, `intent/GatedReadyFixPlan.md`, `16d349c`): mark_ready/retry refuse gated tasks (live 409s on #183 with instructive text, zero state change); classify surfaces gated waiting_approval/ready/rework as needsyou "Awaiting approval" (live API on #175/#183/#176, the last with "· then waiting on #175"); TaskDetail "Approve → queue" button wired to the audited edit path (live on #175/#183/#176 at 1440 + 390 `isMobile`, scrollWidth 390, no page errors; absent on non-gated failed #156 — Retry shown — and done #174); Tasks-tab Needs-you group went 1 → 5 rows; legacy gated-ready claim-guard + refusal + classify tests added, suite **135 passed**; deployed via `service restart`, health 200; #183 re-specced INTEGRATE/VERIFY via audited API edit (task_edited).
- Owner-gated dispatch safeguard 2026-09-03 (`intent/OwnerGatedDispatchPlan.md`; chain #178–#182 through the pipeline itself): #178 plan written, #179 build (claim/candidate/release/promotion predicates + approval auto-resume in `edit_task`, +65-line `test_approval_gate.py` additions), #180 commit, #181 push (`6a0d02f` on origin/main), #182 integrate/verify done — live service restarted on the verified commit, gated-idle-before / auto-resume-after-approval proven, #174 recovered from its dispatch loop and owner-closed. Runs #360s–#367; reviewer sweep same evening closed #85/#86/#89 (reviews #132/#133 approved).
- Phase-gate workflow bundle 2026-09-02 (`intent/PhaseGatePlan.md`; goal #30, tasks #137–#143, every slice built by dispatched coder runs + independent review through the pipeline itself): #137 run-252 integrated (`e22d8aa`, approve-plan endpoint proven live via authenticated 409 on a bogus id); #138 grilling skill VERBATIM (`79195f3` + article fix `26fb19c`; first attempt bounced by review because mid-run answer-file guidance doesn't bind the reviewer — lesson: owner changes to requirements must be edited into description/DoD) + `orchestrator-intake` defers to it; #139 `--phased` PLAN+BUILD chain (`50f4351`; regression: owner-close of plan promotes build; first agent self-restart via broker observed live); #140 consequence-named buttons + feedback-approval warning (`40bd10c`; landed on the desk REVIEW-UNRUN via the `completed=manual` loophole — diff hand-reviewed in session, then #143 filed); #143 manual-verdict-respects-required-review (`99c2ceb`; its own ledger shows the fixed sequence running→needs_review→manual); #141 `docs/workflow.md` + README link (review #100; interview-protocol line corrected post-review to grilling rounds, `170126f`). Ops: hermes-hq-restarter broker installed by the owner (no setuid sudo on this box — sudoers dead end), flag-file restart proven from uid 10000 (pid 30783→37490, health 200). Owner clicks exercised live: goal #30 release, #137/#139/#140/#143 approvals (one relayed via API when a restart-wiped session ate the UI click).
- 2026-08-31 → 2026-09-01 bundles (owner-close, SSE shield, feedback-from-manual + task edit, polish+ops, board seeding, projects parity + portfolio rename, known-limits sweep, approval gate, copy-message buttons, plus the 08-31 Fast/feature sweeps): collapsed — full one-liners live in the git history of `kis/state/current.md` (up to commit `f19ccea`); durable rules live in Knowledge (Approval gate, Owner overrides, Known limits).
- Suite: `tests/backend` **129 passed, 0 failed** (2026-09-02, run as root during `/kis:check` — the "5 pre-existing environment-sensitive failures" recurring in agent run summaries (cron notification isolation, answer-file reuse, updater notification, terminal HOME, integrity fixture) are uid-10000-environment artifacts, NOT real failures; stop re-reporting them as debt. Previous baseline 123 on 2026-09-01; latest: 6 in `test_approval_gate.py` — gate landing/notify, review-then-gate, manual verdict + sticky flag, review-run refusal, edit audit/API; 1 gated-manual classify in `test_status.py`; earlier: 1 in `test_agents.py` — history paging; label parity in `test_status.py`; 7 in `test_owner_close.py` — close/waive/promote, refusals, undepend, goal-delete guards, manual-feedback brief, edit audit/gates/endpoint; 1 in `test_chat.py` — SSE-disconnect shield; 3 in `test_wm_guards.py` — mark_ready guards, auto-read bounds, orphan audit+close; 8 in `test_questions.py` — fence/heuristic/codex-items/answer endpoint/watermark prune/stall label; 3 in `test_push.py` incl. the unread-badge payload; earlier per-file additions live in git history of this line).
- Groups 1–8 proofs (2026-08-29/30 — work core through install/supervisor, incl. Terminal/Memory/Skills/MCP/Schedules/cutover): collapsed — full one-liners live in the git history of `kis/state/current.md` (up to commit `4a2cad7`); plans are marked COMPLETE in `intent/`.
- Cutover 2026-08-29 13:40: `import --force`, first tick claimed review 84 → run #212. Earlier group proofs live in git history of `kis/state`.
