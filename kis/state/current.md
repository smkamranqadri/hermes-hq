# State

## Status
**hermes-hq is the live control plane since 2026-08-29 13:40 UTC** (branch `main`), running as the **s6 service `/run/service/hermes-hq`** since 2026-08-30 (dispatcher ON, `--host 0.0.0.0 --port 9010 --interval 20`). Restart after backend changes: `hermes-hq service restart` (if health then reads 000 and the log says "Waiting for connections to close", force with `s6-svc -k /run/service/hermes-hq`); logs `/var/log/hermes-hq/current`; auto-update daily 05:00 PKT (`service auto-update --show`). Password file `/opt/data/hermes-hq/password` is `test` for the owner's review (generated one kept in `password.prev`). Frontend builds go live without a restart. HTTPS for the phone: `tailscale serve` on the owner's Mac fronts this VM's `:9010` (Knowledge → Web Push). Legacy WM: fully deleted 2026-08-31 (`knowledge/technical.md` → Legacy WM).

## Now
Task: none — **Group 10 ("Needs you" for dispatched runs) is complete as of 2026-08-31**: briefs teach the hq-options fence + answer file, the dispatcher tick scans running-run transcripts (incl. codex_message_items), question notifications push to the phone, Task detail banner + run-owned Chat answer box deliver <runs_dir>/<run>.answer.txt, and the full loop is proven live (run #228: asked → notified → answered → "Owner chose Borealis"). Groups 1–10 all shipped. Next session: `/kis:plan` for the owner's pick.

## Next
Owner's pick — plan first.

## Blocker
None.

## Known debt (open items only; accepted limits live in Knowledge → Known limits)
- Agent detail history shows 30 rows + "Show more" (40 at a time) over the reader's 120-row cap; the cap itself is unchanged.
- Specialist `.env` files carry the root `API_SERVER_KEY` (template-inherited); per-profile keys are an owner choice.
- SSE disconnect kills live chat turns: when the browser drops the stream (phone lock, tab switch, nav), the Hermes gateway hard-interrupts the running turn (`_INTERRUPT_REASON_SSE_DISCONNECT`, gateway/run.py:3254; gateway.log 2026-08-31 07:20/07:37) and the engine mislabels it "User sent a new message" → chat shows "Operation interrupted." Fix candidate: backend shields the gateway stream — keep consuming after client disconnect so the turn finishes and lands as a notification.
- `mark_ready` re-readies a RUNNING task → dispatcher double-claims: task #109 (2026-08-31) was auto-readied on creation (released goal, deps done) and claimed (run #229); an explicit `wm task mark-ready` then ran `* -> ready` on the running task and the next tick claimed it again (run #230) — two coder runs raced the same repo. Fix: `mark_ready` should refuse when status is 'running' (and no-op on 'ready'); `wm_store.py:1268` uses `from_status='*'` with only a `!= 'done'` guard.
- No sanctioned owner-close: work finished outside WM runs (owner deployed the site) cannot become `done` without tripping the integrity audit — tasks #84/#109 sit as `manual` forever, and `deps_done` requires `done`, so dependents like #89 never auto-release. Fix candidates: `wm task close --by-owner` (audited transition that satisfies deps) + a dep repoint/remove CLI.
- Owner feedback is refused on `manual`/`ready` tasks and there is no task-edit endpoint — the only way to thread new owner context into a brief is superseding with a new task (worked, but heavyweight for a description fix). Fix candidates: allow `owner_feedback` from `manual`, or an audited description/DoD edit.
- Needs-you notifications for a task stay unread after the task leaves the flagged state (post-launch: 9 stale rows for already-resolved #84/#96/#106/#109). Fix candidate: auto-read a task's needs_you/question rows on its next transition.
- Orphan review row: review #29 (task #63) stuck `reviewed` with NULL verdict on a done task — `wm check` doesn't flag it. Fix candidate: integrity finding + close path.

## How to run
`README.md`. Dev: `.venv/bin/hermes-hq serve --no-dispatcher` + `cd frontend && npm run dev`. Owner drops reference images in `screenshots/` (git-ignored). Playwright: settings in memory `hermes-hq-ops` (browser path, 390×844 + `isMobile` for phones); scripts are per-session.

## Proof (latest, one line per shipped area — details are in git history)
- Small-debt sweep 2026-08-31 (Fast): (1) `mark_stalled` grew a `label` param — dispatcher keeps `liveness:`, `stop.py` passes `owner stop:`; test in `test_questions.py` pins both strings; suite **105 passed**. (2) #96 closed as `manual` with a note pointing at #98 (spec already implemented and live) — owner decision, no re-feed. (3) SOUL-overlay row was stale: root `/opt/data/SOUL.md` is byte-identical to the template, starts with the `# Orchestrator` marker (`overlay_applied()` true), `hermes-hq-ops` skill live — dropped, nothing to apply.
- Files artifacts grouped by task 2026-08-31 (Fast; `Files.tsx` Artifacts component only — API already carried task_id/title/agent/run): per-task collapsible headers "#id · title" + count chips, newest task first, task-less runs under "other runs", groups auto-open when the whole list ≤ 8; outer toggle (`data-artifacts-toggle`) unchanged. Live proof 1440 + 390 `isMobile` on personal-brand (now 41 artifacts): 14 groups, counts sum 41, default collapsed, toggle opens, row click opens the editor (`proof-assets/ui-proof-2026-08-31.md`), scrollWidth 390/1440, no page errors; screenshots reviewed.
- Needs-you 10-3 e2e 2026-08-31 (live :9010, real dispatched coder task #108/run #228): claimed 20 s; fence detected from the real transcript at 40 s (`runq:228:10395`, pushed); answer file written + rows marked read; 75 s run done, task summary **"Owner chose Borealis"**. Attempts 1-4 each surfaced a real fix: writer never writes reply text in `-Q` (tool-only); deleted proof rows caused SQLite id reuse so the `wm-run-<id>` marker re-attached an old session (**never delete task/run rows — park as manual instead**); a reply that ends after asking ends the one-shot run (brief now says continue with tools in the same reply); codex-style models keep pre-tool reply text in `codex_message_items` with empty `content` — scan reads both. e2e tasks #105-108 kept (105/107/108 done, 106 manual), notifications read; suite **104 passed**.
- Needs-you 10-2 2026-08-31: design amended (owner: Hybrid) — gateway steer cannot reach dispatched CLI runs (in-process `_pending_steer`, verified in /opt/hermes), so answers go to `<runs_dir>/<run>.answer.txt` via `POST /api/run/{id}/answer` (running-only, 404/409 guards, marks runq rows read; brief names the file). `test_questions.py` 6 passed, suite **103 passed**. Live (synthetic run 225 on a real coder session, dispatcher paused — liveness had instantly failed the fake run unpaused; rows + file cleaned, resumed): phone 390 task banner "QUESTION Deploy to staging or prod? · Answer in session" → run-owned chat shows working-here + answer box → POST 200, toast, file got both owner lines; desk banner shown 1440; screenshots reviewed.
- Needs-you 10-1 2026-08-31: `test_questions.py` 5 passed (fence parsed → question row with body/href/dedupe + watermark; heuristic capped 1/run; orchestrator marker session in root state.db; missing db/session never raises; finished-run watermark pruned; brief carries ASKING THE OWNER before the completion contract); full suite **102 passed**. Scan pushes its own rows (`sync_and_push` covers only transition rows — chat pattern followed).
- PWA 9-4 2026-08-31 (Playwright on live :9010, 390 `isMobile` + 1440, task #98 with 63 runs): phone Folds (Runs/Reviews/History, `data-fold`) all collapsed on load — page 3022 px vs 11683 px with Runs open; tap → expands; RunLog stays visible by design; Activity "Load older" 100 → 200 rows (feature existed since cutover `9fb2cac` — stale debt row, proof-only); scrollWidth 390 on both pages; desktop: 0 fold buttons visible, runs/reviews content shown with no clicks (screenshots reviewed); no page errors.
- PWA 9-3 offline 2026-08-31 (Playwright 390 `isMobile` on live :9010): cache `hq-offline-v1` holds exactly `/offline.html`; offline navigation → branded offline page (title + h1 asserted, screenshot reviewed); `/api/notifications` while offline → network error (never cached); back online → live Tasks + live API; sw controls the page after one update+reload; no page errors. Serving note: the SPA catch-all returns real files first, so `/offline.html` and `/sw.js` come from `backend/static`.
- PWA 9-2 badge 2026-08-31: `test_push.py` 3 passed (new: payload carries live `unread`; monkeypatched send captured `[2, 2]`); live :9010 after restart (Playwright 390 `isMobile`, stubbed Badging API): load with 1 unread → `setAppBadge(1)`, Mark all read → `clearAppBadge()` + API unread 0; zero-unread load → `clearAppBadge()`; proof notification row deleted after; sw.js badge-on-push is code-only here (headless has no push service) — owner checks on the phone.
- PWA 9-1 2026-08-31 (Playwright on live :9010, 390×844 `isMobile` + 1440): phone toast bottom 772 above tab bar top 778 (real error toast via the push toggle — headless denial), desktop toast container `bottom` 16 px unchanged, `overscroll-behavior-y: none` computed on html+body, scrollWidth 390, no page errors; `More.tsx` confirmed static (no skeleton needed); screenshots reviewed.
- Suite: `tests/backend` **105 passed** (2026-08-31; latest: 8 in `test_questions.py` — fence/heuristic/codex-items/answer endpoint/watermark prune/stall label, 3 in `test_push.py` incl. the unread-badge payload; earlier per-file additions live in git history of this line).
- Groups 1–8 proofs (2026-08-29/30 — work core through install/supervisor, incl. Terminal/Memory/Skills/MCP/Schedules/cutover): collapsed — full one-liners live in the git history of `kis/state/current.md` (up to commit `4a2cad7`); plans are marked COMPLETE in `intent/`.
- Cutover 2026-08-29 13:40: `import --force`, first tick claimed review 84 → run #212. Earlier group proofs live in git history of `kis/state`.
