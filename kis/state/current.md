# State

## Status
**hermes-hq is the live control plane since 2026-08-29 13:40 UTC** (branch `main`), running as the **s6 service `/run/service/hermes-hq`** since 2026-08-30 (dispatcher ON, `--host 0.0.0.0 --port 9010 --interval 20`). Restart after backend changes: as root `hermes-hq service restart` (if health then reads 000 and the log says "Waiting for connections to close", force with `s6-svc -k /run/service/hermes-hq`); **agents (uid 10000) restart via the `hermes-hq-restarter` broker** — `touch /run/hermes-hq-restart/restart.request`, 30 s cooldown (own s6 service, boot-persistent; installed 2026-09-02 because this box has no setuid sudo). Restarts wipe in-memory login sessions — the owner's UI needs a re-login after each. Logs `/var/log/hermes-hq/current`; auto-update daily 05:00 PKT (`service auto-update --show`). Password file `/opt/data/hermes-hq/password` is `test` for the owner's review (generated one kept in `password.prev`). Frontend builds go live without a restart. HTTPS for the phone: `tailscale serve` on the owner's Mac fronts this VM's `:9010` (Knowledge → Web Push). Legacy WM: fully deleted 2026-08-31 (`knowledge/technical.md` → Legacy WM).

## Now
Task: none — **phase-gate workflow bundle COMPLETE 2026-09-02** (`intent/PhaseGatePlan.md`; goal #30, all 7 tasks done same day, proof below). The owner-approved workflow is live: grilling interview-first intake (Matt Pocock's skill verbatim + `orchestrator-intake` deferring to it), `--phased` PLAN+BUILD chain creation (CLI + UI), consequence-named approve buttons + feedback-approval warning, `completed=manual` now routes through required review before the owner's desk (#143, loophole found live on #140), `docs/workflow.md` for new users. Agents restart the service themselves via the **hermes-hq-restarter broker** (see Status). Board otherwise unchanged: #82/#85/#86 `manual`, #89 parked, **#130 still `manual` on the owner's desk** (to be re-modeled as a phase chain under the new workflow rather than unstuck ad hoc — owner decision 2026-09-02).

## Next
**Owner is live-testing the new intake** (fresh orchestrator chat, rough idea → grilling rounds → scope verdict → phased creation) — audit the created rows against the recipe when he reports back. Candidate follow-ups surfaced, not yet interviewed: persist login sessions across service restarts (restarts log the owner out; sessions are in-memory), and an optional task-first intake variant (rough task parked `planned`, interview in its task chat). #130 re-modeling per the new workflow.

## Blocker
None.

## Known debt (open items only; accepted limits live in Knowledge → Known limits)
None.

## How to run
`README.md`. Dev: `.venv/bin/hermes-hq serve --no-dispatcher` + `cd frontend && npm run dev`. Owner drops reference images in `screenshots/` (git-ignored). Playwright: settings in memory `hermes-hq-ops` (browser path, 390×844 + `isMobile` for phones); scripts are per-session.

## Proof (latest, one line per shipped area — details are in git history)
- Phase-gate workflow bundle 2026-09-02 (`intent/PhaseGatePlan.md`; goal #30, tasks #137–#143, every slice built by dispatched coder runs + independent review through the pipeline itself): #137 run-252 integrated (`e22d8aa`, approve-plan endpoint proven live via authenticated 409 on a bogus id); #138 grilling skill VERBATIM (`79195f3` + article fix `26fb19c`; first attempt bounced by review because mid-run answer-file guidance doesn't bind the reviewer — lesson: owner changes to requirements must be edited into description/DoD) + `orchestrator-intake` defers to it; #139 `--phased` PLAN+BUILD chain (`50f4351`; regression: owner-close of plan promotes build; first agent self-restart via broker observed live); #140 consequence-named buttons + feedback-approval warning (`40bd10c`; landed on the desk REVIEW-UNRUN via the `completed=manual` loophole — diff hand-reviewed in session, then #143 filed); #143 manual-verdict-respects-required-review (`99c2ceb`; its own ledger shows the fixed sequence running→needs_review→manual); #141 `docs/workflow.md` + README link (review #100; interview-protocol line corrected post-review to grilling rounds, `170126f`). Ops: hermes-hq-restarter broker installed by the owner (no setuid sudo on this box — sudoers dead end), flag-file restart proven from uid 10000 (pid 30783→37490, health 200). Owner clicks exercised live: goal #30 release, #137/#139/#140/#143 approvals (one relayed via API when a restart-wiped session ate the UI click).
- Copy-message buttons 2026-09-01 (#135, coder run in-place): per-bubble copy button + shared `copyText` helper with legacy execCommand fallback (code-block copy reuses it); live in every frontend build since, but sat UNCOMMITTED in main until 2026-09-02 (`647879e`) — pre-phase-gate briefs didn't demand commits; today's briefs do.
- Approval-gate bundle 2026-09-01 (Standard, `intent/ApprovalGatePlan.md`): (A) `owner_approval` column (additive migration, applied live) gates completions to `manual`/"Awaiting approval"/needsyou — engine paths `record_completion` (policy none) + `review_verdict` approved (no promote; detail "review approved — awaiting owner approval"); CLI `--owner-approval` on create/edit, API edit field, TaskDetail gate-chip toggle + "Approve → done" button. (B) contract verdict `manual` (4th value; review runs refused via `_finalize` guard; hand-over SETS the flag — sticky gate). (C) `files.resolve_artifact` + task `artifacts`; TaskDetail Results rows expand inline (`.md` rendered via chat Markdown, 88 md elements on #130's real plan; "Open in Files" deep link; images via raw). (D) retrofit `owner_approval=1` on #122/#123/#124/#127 with `task_edited` audits. Suite **123 passed** (6 new in `test_approval_gate.py` incl. no-review-spawn, review-then-gate, sticky flag, review-run refusal, edit audit + API). Live e2e on :9010 with REAL dispatched coder runs: gated #131 (brief carried "OWNER APPROVAL GATE" + 4-verdict contract) run #234 done → task `manual`, unread needs_you, artifact resolved → approved via close-owner; #132 run #235 completed `manual` with blocker → flag auto-set → feedback "Apollo" → rework run #236 wrote apollo-choice.txt + completed done → sticky gate caught it → `manual` again → approved. Playwright 390 `isMobile` + 1440: "Awaiting approval" chip in the Needs-you group (Tasks tab, #134 scratch in active project — archived-project tasks don't appear on the global tab, which is why #131/#132 couldn't prove it), gate chip + Approve button on detail, #130's plan rendered inline, scrollWidth 390, no page errors; screenshots reviewed. Scratch #131–134 owner-closed, proof notifications read, scratch files removed, `wm check` Integrity OK.
- Known-limits cleanup sweep 2026-09-01 (Fast): `tests/core/test_t2/t5/t7` fixed for the Phase-6.5 goal lifecycle (advance `draft→planning→planned` before `release_goal`) — all core scripts green (t1 46 / t2 8 / t3 12 / t4 10 / t5 24 / t6 22 / t7 32, 0 fail); surfaced+fixed an undocumented t1 bug (its CLI section wrote to the LIVE hq.db via `wm_cli`'s second `core.wm_store` instance — t1 now sets `WM_DB`; details in Knowledge → Known limits); `backend/readers.py` audited, dead `goal_detail` + `list_transitions` deleted (1083→1055). Suite **117 passed**; service restarted, smoke 200s, log clean.
- Projects parity + portfolio rename 2026-09-01 (Fast, owner-confirmed at each step): live-DB audit read-only (13 test-era projects already archived, 14 open tasks under them all `planned` — no tidy needed, no rows touched); 10 junk folders moved from `/opt/data/projects` to **`/opt/data/.projects-trash-2026-09-01`** (owner can purge anytime); end state **5 folders ↔ 5 active projects 1:1** — `personal-brand` #7, `riyadh-founding-engineer` #9 (restored), `social-media-content` #12 (restored), `hermes-hq` #17 (new — the app tracks itself), `kamranqadri-portfolio` #18 (new; folder + project + GitHub repo all renamed to the corrected spelling via `gh repo rename`, old URL redirects, local remote + script refs updated). `/api/projects?archived=0` = exactly those five.
- Board seeding 2026-09-01 (owner-approved drafts): goal #24 hermes-hq build history (5 tasks #112–116 seeded manual→owner-closed done, goal released only AFTER all children done so nothing promoted); goal #25 riyadh outreach (3 done history #117–119 + 5 parked execution #120–124 with wave deps); #26/#27/#28 social-media-content chain (#125–127); #29 portfolio upkeep (#128–129). Non-history goals parked `planned`; `wm check` OK, zero `ready` tasks at seed time.
- Polish+ops bundle 2026-09-01 (Standard, `intent/PolishOpsPlan.md`): (A) manual chip reads **"Handed over"** via optional `human.label` (classify + status.ts mirror + StatusBadge + TaskDetail passthrough; parity test pins both layers; grouping unchanged — Tasks tab shows 3 handed-over rows, Done chips untouched). (B) six specialist gateway keys rotated to distinct tokens (analyst/writer/coder/reviewer/marketer/uiux — marketer+uiux also carried root; researcher not installed), none equal root; live proof: coder gateway enabled → session created with NEW key, OLD root key → 401, disabled after; repo templates ship no literal key so fresh installs auto-generate. (C) `GET /api/agent/{name}?history=N` (default 120, max 1000, 422 above) + the **previously-missing Show-more button** (the original commit shipped `shown=30` with no button — history was hard-capped at 30 visible): live paging 30→70→96 on coder, button hides when exhausted; >120 case pinned by a 150-row unit test (no live agent exceeds 120 yet). (D) #82/#85/#86 descriptions/DoD rewritten to the FINAL owner positioning (owner approved all three drafts verbatim; `task_edited` audit rows with old values; tasks stay `manual`; #89 untouched, `waiting_approval`). Suite **117 passed**; Playwright 390 `isMobile` + 1440: chips correct on #82/#109/Tasks tab, scrollWidth 390, no page errors; screenshots reviewed.
- Feedback-from-manual + task edit 2026-08-31 (Standard, `intent/FeedbackEditPlan.md`): `OWNER_FEEDBACK_SOURCE_STATUSES` + `'manual'` (manual → rework, words in the marker/brief); `edit_task` (description/DoD only, refused running/done, activity `task_edited` keeps OLD values), `wm task edit --description/--dod`, `POST /api/task/{id}/edit`, TaskDetail pencil-edit on both blocks (hidden running/done). Suite **116 passed** (3 new in `test_owner_close.py`: manual-feedback brief words via render_brief, edit audit+gates, endpoint 200/409). Live :9010 (dispatcher paused for the rework window, resumed + verified `paused: False` after): scratch #110 manual → API edit persisted → feedback → rework (`owner feedback:` marker) → re-manual → owner-closed; edit-on-done 409. Playwright scratch #111, 390 `isMobile` + 1440: feedback button + 2 pencil edits on manual, textarea 16 px, save → v2 text survives reload, 0 edit buttons on done #109, scrollWidth 390, no page errors; label-merge nit fixed (✎ glyph) and re-screenshotted. #110/#111 owner-closed, notifications read, `wm check` green.
- SSE stream shield 2026-08-31 (Standard, `intent/SseShieldPlan.md`): `stream_turn` gateway read-loop moved to a daemon pump thread feeding the response generator via queue; client disconnect (GeneratorExit) sets `client_gone` and only stops delivery — the pump drains the gateway to completion (bounded by TURN_TIMEOUT 1 h) then fires `_notify_turn_done` with the full reply. Test: FakeGateway `finished` flag — generator closed after 4 lines, gateway still wrote every event, unread `chat` notification carried the full body; suite **113 passed**. Live :9010 + real orchestrator gateway: dropped the client at +0.0 s after `message.started` on a sleep-8 turn → turn completed, unread notification "SHIELD PROOF COMPLETE.", transcript intact, and `gateway.log`/`agent.log` show NO "SSE client disconnected; interrupted" for it (the only such lines remain the original 07:20/07:37 incidents). Proof session deleted + notification read after.
- Owner-close pass 2026-08-31 (Standard, `intent/OwnerClosePlan.md`): `close_by_owner` (manual-only; done transition + `task_closed_by_owner` activity + waives open reviews + promotes dependents), `wm task close --by-owner`, `POST /api/task/{id}/close-owner`, TaskDetail "Close as done" (manual status only), `wm task undepend` (re-checks release eligibility; repoint = undepend+depend), guarded `wm goal delete` (draft + unreferenced only; activity FK made the delete detach `activity.goal_id` in-transaction, audit row carries the id in detail). Suite **112 passed** (4 new in `test_owner_close.py`). Live :9010 after restart: 12 closes with notes (#3/22/23/84/96/98/99/101/102/104/106/109), goal #15 deleted, `wm check` green before AND after, promoted dependents "-" every close (#89 stayed parked behind #82/#85/#86 as designed). Playwright phone 390 `isMobile` + desk 1440: button on manual #82, absent on done #109 (no Take over either), scrollWidth 390/1440, no page errors; screenshots reviewed.
- 2026-08-31 Fast/feature sweeps (WM-fix guards, small-debt, Files artifact grouping, Needs-you 10-1/10-2/10-3 e2e, PWA 9-1..9-4): collapsed — full one-liners live in the git history of `kis/state/current.md` (up to commit `416f571`); the durable lessons (never delete task/run rows, answer-file design, codex transcript quirks) live in Knowledge.
- Suite: `tests/backend` **129 passed, 0 failed** (2026-09-02, run as root during `/kis:check` — the "5 pre-existing environment-sensitive failures" recurring in agent run summaries (cron notification isolation, answer-file reuse, updater notification, terminal HOME, integrity fixture) are uid-10000-environment artifacts, NOT real failures; stop re-reporting them as debt. Previous baseline 123 on 2026-09-01; latest: 6 in `test_approval_gate.py` — gate landing/notify, review-then-gate, manual verdict + sticky flag, review-run refusal, edit audit/API; 1 gated-manual classify in `test_status.py`; earlier: 1 in `test_agents.py` — history paging; label parity in `test_status.py`; 7 in `test_owner_close.py` — close/waive/promote, refusals, undepend, goal-delete guards, manual-feedback brief, edit audit/gates/endpoint; 1 in `test_chat.py` — SSE-disconnect shield; 3 in `test_wm_guards.py` — mark_ready guards, auto-read bounds, orphan audit+close; 8 in `test_questions.py` — fence/heuristic/codex-items/answer endpoint/watermark prune/stall label; 3 in `test_push.py` incl. the unread-badge payload; earlier per-file additions live in git history of this line).
- Groups 1–8 proofs (2026-08-29/30 — work core through install/supervisor, incl. Terminal/Memory/Skills/MCP/Schedules/cutover): collapsed — full one-liners live in the git history of `kis/state/current.md` (up to commit `4a2cad7`); plans are marked COMPLETE in `intent/`.
- Cutover 2026-08-29 13:40: `import --force`, first tick claimed review 84 → run #212. Earlier group proofs live in git history of `kis/state`.
