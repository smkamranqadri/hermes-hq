# State

## Status
**hermes-hq is the live control plane since 2026-08-29 13:40 UTC** (branch `main`), running as the **s6 service `/run/service/hermes-hq`** since 2026-08-30 (dispatcher ON, `--host 0.0.0.0 --port 9010 --interval 20`). Restart after backend changes: as root `hermes-hq service restart` (if health then reads 000 and the log says "Waiting for connections to close", force with `s6-svc -k /run/service/hermes-hq`); **agents (uid 10000) restart via the `hermes-hq-restarter` broker** — `touch /run/hermes-hq-restart/restart.request`, 30 s cooldown (own s6 service, boot-persistent; installed 2026-09-02 because this box has no setuid sudo). Restarts wipe in-memory login sessions — the owner's UI needs a re-login after each. Logs `/var/log/hermes-hq/current`; auto-update daily 05:00 PKT (`service auto-update --show`). Password file `/opt/data/hermes-hq/password` is `test` for the owner's review (generated one kept in `password.prev`). Frontend builds go live without a restart. HTTPS for the phone: `tailscale serve` on the owner's Mac fronts this VM's `:9010` (Knowledge → Web Push). Legacy WM: fully deleted 2026-08-31 (`knowledge/technical.md` → Legacy WM).

## Now
Task: **Second Brain 2b-i — review loop polish — IN PROGRESS 2026-09-05** (branch `second-brain-2b-i`; verification = suite + live Playwright 390/desktop + live librarian run) (Phase mode, direct in-session; scope + locked decisions in `intent/SecondBrainPlan.md` → 2b-i: contradiction + new-task kinds, edit-before-approve + feedback-on-reject, archive-via-file + ARCHIVE chip, ingest nudge + Triage-now button, roster refresh; defer CUT, wiki-update → Phase 4, 2b split into 2b-i/2b-ii). Start = `/kis:act`.
Prior task: **Second Brain 2a SHIPPED + CLOSED 2026-09-06** (owner sign-off "looks good"; `intent/SecondBrainPlan.md` 2a marked done). The librarian is live end-to-end: owner provisioned auth, retry of #190 → run 397 was its **first real model run** (triaged a capture, filed a sane needs_attention proposal unaided); ingest schedule #2 (heartbeat, */30) is ENABLED. Same-day owner-driven follow-ups all shipped+deployed: **agent model selection** (`49daf11` + hq-select restyle `b218135` — AgentDetail edits the profile's default provider/model/effort through Hermes' own assignment code via bridge `model_get`/`model_set`; audited; applies to new runs/sessions), **Memory link + template memory seeds** (`44c047b` — librarian seeded in the specialists' § style; seeds install only when the profile has no MEMORY.md), and the **proposal-visibility bundle** (`bc7d279` inbox chip, `df207a1` note-page ProposalBanner with inline approve/reject) after three owner confusions over one proposal — rule now in Knowledge → rules. Pending on the owner's desk: proposal #2 on note #8 (junk test capture → Journal) awaits approve/reject.
Earlier: **gated-ready fix SHIPPED 2026-09-04** (`intent/GatedReadyFixPlan.md`, commit `16d349c`, deployed live; proof below) — pipeline verification pending: **#183 is re-specced as INTEGRATE/VERIFY** (audited edit; independently verify the deployed fix) and awaits the owner's Approve → queue click. Board: #175 gated-ready (legacy row — approving dispatches it), #176/#177 gated `waiting_approval` (now visible as "Awaiting approval" in Needs-you), **#156 `failed`** (namazee APK — arm64 box can't run the x86_64 NDK/AAPT2 toolchain; Knowledge → Known limits; owner decision: park or move to x86_64 CI). Earlier 2026-09-03: safeguard chain #178–#182 complete (`6a0d02f`), #174 recovered, #82/#85/#86/#89/#130 done.

## Next
**Implement 2b-i via `/kis:act`** (plan approved this session; 2b-ii planned after 2b-i ships). Owner micro-decision open: proposal #2 (note #8) approve/reject. Push drift cleared 2026-09-05: `git push origin main` done (origin at `3e5db3f`); auto-update unblocked.
P1+2a shipped + owner-reviewed 2026-09-05/06 (`54327b0` `e2e796e` `27446b3` `a48f246`; mockup https://claude.ai/code/artifact/28097e1a-2343-442f-a21e-ed014e507eb3).
Parked 2026-09-05: **DocsPlan** (`intent/DocsPlan.md` — user docs at `/help`, MkDocs Material, docs DoD gate) — resume = `/kis:act`.
Also: **Owner clicks "Approve → queue" on #183** (launches the independent verify run through the pipeline, review required), then decides #175 (starts LinkedIn/Medium file review) and #176/#177. Still uninterviewed candidates: persist login sessions across restarts, task-first intake variant.

## Blocker
None.

## Known debt (open items only; accepted limits live in Knowledge → Known limits)
- **Worktree lifecycle has no cleanup owner — investigate + strengthen the workflow.** Found 2026-09-02: the engine creates a worktree+branch per `is_code` run but NOTHING ever removes them — 5 stale registered worktrees (#136's three dirty attempts + #139/#143's merged ones), 2 empty orphan dirs, and 49 dangling symlinks (2026-08-29 importer linked legacy `/opt/data/work-manager` worktrees; the 08-31 legacy purge deleted the targets but never swept the hq side) accumulated silently until a manual sweep (dirty diffs parachuted: `/opt/data/worktree-diffs-2026-09-02.tar.gz`). Also: done-task `result_paths` point INTO worktrees, so cleanup breaks old artifact links (#136/#139/#143 now show "missing on disk"). Fix candidates for an intake interview: engine-side GC (remove worktree+branch when the task is done AND its branch is merged — or a `wm gc` command with a parachute), briefs already demand merge but not self-cleanup, importer/purge procedures must sweep symlinks, and completions should prefer main-checkout paths in `result_paths` after merging.

## How to run
`README.md`. Dev: `.venv/bin/hermes-hq serve --no-dispatcher` + `cd frontend && npm run dev`. Owner drops reference images in `screenshots/` (git-ignored). Playwright: settings in memory `hermes-hq-ops` (browser path, 390×844 + `isMobile` for phones); scripts are per-session.

## Proof (latest, one line per shipped area — details are in git history)
- 2a closure + visibility bundle 2026-09-06: librarian's first REAL model run (task #190 retry → run 397 done; session `20260905_131248_170a7b`) read the inbox via `wm note`, filed needs_attention proposal #2 (`{"area_id": 8}` Journal) with an honest summary, and its completion summary matched the DB state; schedule #2 resumed via API (`enabled` 0→1); memory link + template seeds verified live (librarian MEMORY.md rendered at `/memory?profile=librarian`); inbox chip + note-page ProposalBanner verified at 390 (banner text carries summary + JOURNAL chip + Approve/Reject, scrollWidth 390, 0 page errors); suite **168 passed 0 failed** (+1 pending-mark test, +1 template-memory seed/no-clobber test on top of the +5 model-selection tests); deployed `bc7d279`→`df207a1`, restarts healthy 200.
- Agent model selection 2026-09-05 (Standard, `49daf11`, direct in-session; owner-interviewed scope: profile default, provider+model+effort): suite **166 passed 0 failed** (+5 `test_agent_model.py` over a faked bridge — owner wall 401, 404s, provider+model pairing, closed effort ladder, bridge-refusal passthrough, audit row, effort-only write, expensive-confirm loop applies nothing until confirmed); live: GET `/api/agent/writer/model` returned the real config via the real bridge (gpt-5.6-luna-900k / openai-codex / medium), `/api/chat/models?profile=writer` fed the modal (4 providers with "(current)", 10 suggestions, 8 efforts — desktop screenshots), 390×844 librarian effort-only save round-tripped (scrollWidth 390, 0 page errors), `hermes_cli.config.load_config` under the librarian HOME read back `reasoning_effort: medium` while writer's model cfg stayed byte-identical, audit row `agent_model_set librarian -/- effort=medium`; deployed via ff-merge + frontend build + `service restart`, health 200.
- Second Brain P2a 2026-09-05 (Phase, `intent/SecondBrainPlan.md`, `a48f246`, direct in-session build; deployed via ff-merge to main + frontend build + `service restart`, health 200, live DB migrated — `proposals` table + `schedules.heartbeat` verified present): suite **161 passed 0 failed** (+16 `test_proposals.py`: owner-session wall 401/403, librarian-surface-can't-write-notes incl. CLI-verb closure, payload validation, supersede, notify, file+split+keep-original approval round trips, atomic stale-payload 409, approve-routine bulk, CLI propose e2e, heartbeat skip/fire/validate, librarian assignable); LIVE round trip on :9010 — batch dump captured through the real 390×844 `isMobile` CaptureBox, `wm note propose-split` filed proposal #1, Review queue showed it (sub-nav "Review · 1", scrollWidth 390, 0 page errors), Approve created notes #4–7 filed to Health/Work/Journal/kamranqadri-portfolio with the original archived, Urdu FTS hits the new note, needs_you notification → `/brain/review`, `wm check` integrity OK, desktop 1440 approved-history + Library area view verified; LIVE heartbeat — scheduled tick recorded `skipped / "heartbeat: nothing new (0 untriaged inbox note(s))"`, NO task minted, next_fire_at advanced; `librarian` profile installed via the real add-agent flow (port 8656 by append — SPECIALIST_PROFILES order is load-bearing, append only); schedule #2 created via API with `heartbeat=librarian_ingest`, then paused pending owner auth. Screenshots in the job tmp (`shot-0[1-6]*.png`).
- Gated-ready fix 2026-09-04 (Standard, `intent/GatedReadyFixPlan.md`, `16d349c`): mark_ready/retry refuse gated tasks (live 409s on #183 with instructive text, zero state change); classify surfaces gated waiting_approval/ready/rework as needsyou "Awaiting approval" (live API on #175/#183/#176, the last with "· then waiting on #175"); TaskDetail "Approve → queue" button wired to the audited edit path (live on #175/#183/#176 at 1440 + 390 `isMobile`, scrollWidth 390, no page errors; absent on non-gated failed #156 — Retry shown — and done #174); Tasks-tab Needs-you group went 1 → 5 rows; legacy gated-ready claim-guard + refusal + classify tests added, suite **135 passed**; deployed via `service restart`, health 200; #183 re-specced INTEGRATE/VERIFY via audited API edit (task_edited).
- 2026-08-31 → 2026-09-03 bundles (owner-close, SSE shield, feedback-from-manual + task edit, polish+ops, board seeding, projects parity + portfolio rename, known-limits sweep, approval gate, copy-message buttons, the 08-31 Fast/feature sweeps, phase-gate workflow bundle 09-02 incl. the restarter broker, owner-gated dispatch safeguard 09-03 chain #178–#182): collapsed — full one-liners live in the git history of `kis/state/current.md` (up to commit `a8c28cf`); durable rules live in Knowledge (Approval gate, Owner overrides, Known limits, DoD-edit rule).
- Suite: `tests/backend` **168 passed, 0 failed** (2026-09-06 baseline; per-file test-addition ledger lives in the git history of this line; the recurring uid-10000 "environment-sensitive failures" rule → Knowledge → technical.md → Backend test suite).
- Groups 1–8 proofs (2026-08-29/30 — work core through install/supervisor, incl. Terminal/Memory/Skills/MCP/Schedules/cutover): collapsed — full one-liners live in the git history of `kis/state/current.md` (up to commit `4a2cad7`); plans are marked COMPLETE in `intent/`.
- Cutover 2026-08-29 13:40: `import --force`, first tick claimed review 84 → run #212. Earlier group proofs live in git history of `kis/state`.
